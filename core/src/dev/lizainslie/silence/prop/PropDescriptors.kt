package dev.lizainslie.silence.prop

/**
 * Prop descriptors are baked into map and object models and allow the game to
 * determine prop placement inside the map and differentiate objects.
 *
 * @author Liz Ainslie
 */
object PropDescriptors {
    const val BLUEPRINT = "prop_blueprint"
    const val CRATE = "prop_crate"
    const val LOOTBOX = "prop_lootbox"
}