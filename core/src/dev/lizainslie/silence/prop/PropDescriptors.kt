package dev.lizainslie.silence.prop

/**
 * Prop descriptors are baked into map models and allow the game to determine
 * prop placement inside the map.
 *
 * @author Liz Ainslie
 */
object PropDescriptors {
    const val BLUEPRINT = "prop_blueprint"
    const val CRATE = "prop_crate"
}